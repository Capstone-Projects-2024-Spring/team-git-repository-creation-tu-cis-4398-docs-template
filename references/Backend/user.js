/** 
 * Function to validate login by getting the username & password, based on response display any errors/messages front end
 * @param {string} username - User's username
 * @param {string} password - User's password
 * @returns {boolean} - Outputs true or false depending if user is successful logging in
 */
function validateLogin(username, password){
    // Implementation here
}

/**
 * Function to validate sign up by getting the username & password, based on response display any errors/messages front end
 * @param {string} username - Desired username for the new account
 * @param {string} email - Desired email for the new account
 * @param {string} password - Desired password for the new account
 * @returns {boolean} - Outputs true or false depending on if the sign-up was successful
 */
function validateSignUp(username, email, password){
    // Implementation here
}

/**
 * Function to save user's language track
 * @param {string} username - User's username
 * @param {string} language - Desired language for the account
 * @returns {array} - Outputs array of languages saved
 */
function saveLanguageTrack(username){
     // Implementation here
}

/**
 * Function to update user's progress on the language track
 * @param {int} lessonID - User's lesson ID
 * @param {string} lessonStatus - User's lesson status
 * @returns {array} - Outputs array of user's updated progress
 */

function updateProgress(lessonID, lessonStatus){
    // Implementation here
}