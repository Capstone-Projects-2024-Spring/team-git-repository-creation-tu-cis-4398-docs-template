import axios from 'axios';

/*
const AddSavedTrip = async (locations) => {
    try {
        const response = await axios.post('https://example.com/api/add-saved-trip', {
            locations
        });

        return response.data;
    } catch (error) {
        console.error('Error adding saved trip:', error.message);
        throw error;
    }
};
*/

export default AddSavedTrip;