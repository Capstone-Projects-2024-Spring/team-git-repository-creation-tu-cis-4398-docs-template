import axios from 'axios';

/*
const FetchMap = async (locations, polyline) => {
    try {
        const response = await axios.post('https://example.com/api/fetch-map', {
            locations,
            polyline
        });

        return response.data;
    } catch (error) {
        console.error('Error fetching map:', error.message);
        throw error;
    }
};
*/

export default FetchMap;