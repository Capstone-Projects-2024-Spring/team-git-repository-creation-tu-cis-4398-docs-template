import axios from 'axios';

/*
const FetchSavedTrips = async (username, password) => {
    try {
        const response = await axios.post('https://example.com/api/saved-trips', {
            username,
            password
        });

        return response.data;
    } catch (error) {
        console.error('Error fetching saved trips:', error.message);
        throw error;
    }
};
*/

export default FetchSavedTrips;